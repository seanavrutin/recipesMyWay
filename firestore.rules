rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the API using the Admin SDK, which bypasses these rules.
    // Browsers must never reach Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
